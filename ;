class Property < ActiveRecord::Base

  has_one :address, :dependent => :destroy
  has_many :photos, :as => :imageable

  mount_uploader :photo, PhotoUploader

  accepts_nested_attributes_for :address, :photos

end
